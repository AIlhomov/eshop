<%- include("../partials/header"), {title: title}; %>

<h1>This is the kind of stuff we offer. In other words category</h1>


<p><i>To output the content of the Category table.</i></p>

<table class="tableid">
    <thead>
        <tr>
            <th>Id</th>
            <th>Category</th>
        </tr>
    </thead>
<% for (const row of res) { %>
    <tbody>
        <tr>
            <td><%= row.category_id %></td>
            <td><%= row.name %></td>
        </tr>
    </tbody>
<% }; %>

</table>
<%- include("../partials/footer"); %>
